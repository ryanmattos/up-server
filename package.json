{
  "name": "server",
  "version": "1.0.0",
  "description": "A basic server available for consumption using introweb",
  "main": "server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NODE_ENV=dev && ts-node-dev --transpile-only --ignore-watch node_modules src/server.ts"
  },
  "author": "Ryan Matos",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "ts-node-dev": "^1.1.8"
  }
}
